# Project Rules & Instructions

- **Automatic APK Upload**: After completing changes in every turn or build, automatically compile the debug APK (`gradle assembleDebug`), upload `app-debug.apk` to Gofile via `https://upload.gofile.io/contents/uploadfile`, and provide the verified Gofile download link to the user in the response.
